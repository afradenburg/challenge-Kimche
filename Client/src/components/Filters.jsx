import React from 'react'

export const Filters = ( setSearchFilters, searchfilters, optionFilters ) => {
    // console.log(optionFilters, "opciones")
    // console.log(setSearchFilters)
  return (
    <div>Filters</div>
  )
}
